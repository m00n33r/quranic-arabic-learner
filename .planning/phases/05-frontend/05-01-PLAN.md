---
phase: 05-frontend
plan: 01
type: execute
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tailwind.config.js
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/api/client.ts
  - frontend/src/contexts/AuthContext.tsx
---

<objective>
Создать React приложение с Vite, настроить Tailwind CSS, Axios API client и AuthContext.

Purpose: Фундамент фронтенда — все страницы и компоненты строятся на этой основе.
Output: Работающий dev-сервер на :5173, Tailwind стили, API client с JWT interceptor, AuthContext.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

**Технические решения:**
- Vite + React + TypeScript (не CRA — устарел)
- React Router v6 (не v5, API другой: useNavigate, Outlet, element=)
- Tailwind CSS v3 — встроенная RTL поддержка через dir="rtl" + rtl: префикс
- Axios — для JWT interceptors (не fetch — сложнее перехватывать)
- Контекст авторизации: React Context + localStorage для токена
- Арабский шрифт: Amiri из Google Fonts (читаемый, поддерживает огласовки)
- API base URL: http://localhost:8000/api/v1 (dev), через env variable для prod

**Структура frontend/src/:**
```
api/
  client.ts       # Axios instance + interceptors
  auth.ts         # register, login, getMe
  cards.ts        # getDueCards, reviewCard
  sessions.ts     # startSession, completeSession
contexts/
  AuthContext.tsx  # user state, token, login/logout functions
components/       # (план 05-03)
pages/            # (план 05-02, 05-04)
hooks/
  useAuth.ts      # хук для AuthContext
App.tsx           # Router + Routes
main.tsx          # entry point
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Инициализация Vite + React + установка зависимостей</name>
  <files>
    frontend/package.json,
    frontend/vite.config.ts,
    frontend/tsconfig.json,
    frontend/index.html
  </files>
  <action>
Создать React приложение через Vite в директории frontend/:
```bash
cd /Users/mun/KFU_studies/course_project
npm create vite@latest frontend -- --template react-ts
cd frontend
npm install
```

Установить дополнительные зависимости:
```bash
npm install react-router-dom axios
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```

Настроить frontend/tailwind.config.js:
```js
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        // Амири — арабский шрифт с огласовками
        arabic: ["'Amiri'", "serif"],
        // Noto Sans Arabic — запасной
        arabic2: ["'Noto Sans Arabic'", "sans-serif"],
      },
    },
  },
  plugins: [],
}
```

Обновить frontend/src/index.css:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

/* Подключить арабский шрифт из Google Fonts */
@import url('https://fonts.googleapis.com/css2?family=Amiri:ital,wght@0,400;0,700;1,400&family=Inter:wght@400;500;600;700&display=swap');

/* Базовый шрифт интерфейса */
body {
  font-family: 'Inter', sans-serif;
}

/* Класс для арабского текста с правильным направлением */
.arabic-text {
  font-family: 'Amiri', serif;
  direction: rtl;
  unicode-bidi: embed;
  line-height: 2;  /* Больше межстрочный интервал для огласовок */
}
```

Обновить frontend/index.html — добавить lang и шрифты:
```html
<!doctype html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Quran Arabic Learner</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

Настроить frontend/vite.config.ts — proxy для API в dev:
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
      },
    },
  },
})
```

С proxy — Axios может использовать относительные URL `/api/v1/...` в dev.
В prod — Vercel rewrites будут делать то же самое.
  </action>
  <verify>cd frontend && npm run dev — сервер запускается на http://localhost:5173 без ошибок, страница открывается в браузере</verify>
  <done>Vite dev server на :5173, Tailwind подключён (добавь class="text-blue-500" в App.tsx для проверки), шрифт Amiri загружается</done>
</task>

<task type="auto">
  <name>Task 2: Axios client + AuthContext + App Router</name>
  <files>
    frontend/src/api/client.ts,
    frontend/src/api/auth.ts,
    frontend/src/api/cards.ts,
    frontend/src/api/sessions.ts,
    frontend/src/contexts/AuthContext.tsx,
    frontend/src/hooks/useAuth.ts,
    frontend/src/App.tsx,
    frontend/src/main.tsx
  </files>
  <action>
Создать frontend/src/api/client.ts:
```typescript
import axios from 'axios'

const API_BASE = '/api/v1'

export const apiClient = axios.create({
  baseURL: API_BASE,
  headers: { 'Content-Type': 'application/json' },
})

// Request interceptor — добавить JWT токен к каждому запросу
apiClient.interceptors.request.use((config) => {
  const token = localStorage.getItem('access_token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

// Response interceptor — при 401 очистить токен
apiClient.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem('access_token')
      window.location.href = '/login'
    }
    return Promise.reject(error)
  }
)
```

Создать frontend/src/api/auth.ts:
```typescript
import { apiClient } from './client'

export interface UserResponse {
  id: number
  email: string
  username: string
  is_active: boolean
}

export interface TokenResponse {
  access_token: string
  token_type: string
}

export const authApi = {
  register: (data: { email: string; username: string; password: string }) =>
    apiClient.post<UserResponse>('/auth/register', data),

  login: (email: string, password: string) => {
    const formData = new FormData()
    formData.append('username', email)
    formData.append('password', password)
    return apiClient.post<TokenResponse>('/auth/login', formData, {
      headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
    })
  },

  getMe: () => apiClient.get<UserResponse>('/auth/me'),
}
```

Создать frontend/src/api/cards.ts:
```typescript
import { apiClient } from './client'

export interface CardDue {
  word_id: number
  arabic: string
  arabic_clean: string
  translation_ru: string | null
  frequency: number
  easiness_factor: number
  interval: number
  repetitions: number
  next_review_date: string
  is_new: boolean
}

export interface ReviewResponse {
  word_id: number
  quality: number
  new_ef: number
  new_interval: number
  new_repetitions: number
  next_review_date: string
  is_correct: boolean
}

export const cardsApi = {
  getDueCards: (limit = 20) =>
    apiClient.get<CardDue[]>(`/cards/due?limit=${limit}`),

  reviewCard: (wordId: number, quality: number, sessionId?: number) =>
    apiClient.post<ReviewResponse>(`/cards/${wordId}/review`, {
      quality,
      session_id: sessionId,
    }),
}
```

Создать frontend/src/api/sessions.ts:
```typescript
import { apiClient } from './client'

export interface SessionStart {
  id: number
  user_id: number
  started_at: string
  is_completed: boolean
}

export interface SessionComplete {
  id: number
  cards_reviewed: number
  cards_correct: number
  accuracy: number
  duration_seconds: number
  completed_at: string
}

export const sessionsApi = {
  startSession: () => apiClient.post<SessionStart>('/sessions/start'),
  completeSession: (id: number) => apiClient.post<SessionComplete>(`/sessions/${id}/complete`),
  getSession: (id: number) => apiClient.get(`/sessions/${id}`),
}
```

Создать frontend/src/contexts/AuthContext.tsx:
```typescript
import { createContext, useContext, useState, useEffect, ReactNode } from 'react'
import { authApi, UserResponse } from '../api/auth'

interface AuthContextType {
  user: UserResponse | null
  isLoading: boolean
  isAuthenticated: boolean
  login: (token: string) => Promise<void>
  logout: () => void
}

const AuthContext = createContext<AuthContextType | null>(null)

export function AuthProvider({ children }: { children: ReactNode }) {
  const [user, setUser] = useState<UserResponse | null>(null)
  const [isLoading, setIsLoading] = useState(true)

  // При загрузке — восстановить сессию из localStorage
  useEffect(() => {
    const token = localStorage.getItem('access_token')
    if (token) {
      authApi.getMe()
        .then(res => setUser(res.data))
        .catch(() => localStorage.removeItem('access_token'))
        .finally(() => setIsLoading(false))
    } else {
      setIsLoading(false)
    }
  }, [])

  const login = async (token: string) => {
    localStorage.setItem('access_token', token)
    const res = await authApi.getMe()
    setUser(res.data)
  }

  const logout = () => {
    localStorage.removeItem('access_token')
    setUser(null)
  }

  return (
    <AuthContext.Provider value={{
      user,
      isLoading,
      isAuthenticated: !!user,
      login,
      logout,
    }}>
      {children}
    </AuthContext.Provider>
  )
}

export function useAuth() {
  const ctx = useContext(AuthContext)
  if (!ctx) throw new Error('useAuth must be used within AuthProvider')
  return ctx
}
```

Создать frontend/src/hooks/useAuth.ts (re-export):
```typescript
export { useAuth } from '../contexts/AuthContext'
```

Создать frontend/src/App.tsx — роутинг:
```typescript
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom'
import { AuthProvider, useAuth } from './contexts/AuthContext'

// Placeholder компоненты (заменим в следующих планах)
const LoginPage = () => <div className="p-8 text-center">Login Page (coming soon)</div>
const RegisterPage = () => <div className="p-8 text-center">Register Page (coming soon)</div>
const DashboardPage = () => <div className="p-8 text-center">Dashboard (coming soon)</div>
const StudyPage = () => <div className="p-8 text-center">Study (coming soon)</div>

function ProtectedRoute({ children }: { children: React.ReactNode }) {
  const { isAuthenticated, isLoading } = useAuth()
  if (isLoading) return <div className="flex items-center justify-center h-screen">Загрузка...</div>
  if (!isAuthenticated) return <Navigate to="/login" replace />
  return <>{children}</>
}

function AppRoutes() {
  const { isAuthenticated } = useAuth()
  return (
    <Routes>
      <Route path="/login" element={isAuthenticated ? <Navigate to="/dashboard" /> : <LoginPage />} />
      <Route path="/register" element={isAuthenticated ? <Navigate to="/dashboard" /> : <RegisterPage />} />
      <Route path="/dashboard" element={<ProtectedRoute><DashboardPage /></ProtectedRoute>} />
      <Route path="/study" element={<ProtectedRoute><StudyPage /></ProtectedRoute>} />
      <Route path="/" element={<Navigate to={isAuthenticated ? "/dashboard" : "/login"} />} />
    </Routes>
  )
}

export default function App() {
  return (
    <BrowserRouter>
      <AuthProvider>
        <div className="min-h-screen bg-gray-50">
          <AppRoutes />
        </div>
      </AuthProvider>
    </BrowserRouter>
  )
}
```

Обновить frontend/src/main.tsx:
```typescript
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.tsx'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```
  </action>
  <verify>cd frontend && npm run dev — открыть http://localhost:5173, видим "Login Page (coming soon)", нет ошибок в консоли браузера</verify>
  <done>Dev server работает, роутинг настроен, AuthContext инициализируется, нет TypeScript ошибок (npm run build должен проходить)</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run dev — dev server на :5173 без ошибок
- [ ] npm run build — TypeScript компиляция без ошибок
- [ ] http://localhost:5173 показывает "Login Page" (redirect с /)
- [ ] http://localhost:5173/dashboard редиректит на /login (нет токена)
- [ ] Network tab: запросы к /api/* проксируются на :8000
</verification>

<success_criteria>
- Vite + React + TypeScript + Tailwind настроены
- Axios client с JWT interceptor
- AuthContext восстанавливает сессию из localStorage
- React Router v6 с ProtectedRoute
- Арабский шрифт Amiri подключён
- .arabic-text CSS класс для RTL текста
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend/05-01-SUMMARY.md`:

# Phase 5 Plan 01: Frontend Foundation Summary

**[Substantive one-liner]**

## Accomplishments

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Step

Ready for 05-02-PLAN.md
</output>
