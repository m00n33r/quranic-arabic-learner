---
phase: 05-frontend
plan: 02
type: execute
depends_on: ["05-01"]
files_modified:
  - frontend/src/pages/LoginPage.tsx
  - frontend/src/pages/RegisterPage.tsx
  - frontend/src/App.tsx
---

<objective>
Создать страницы авторизации — Login и Register с валидацией и обработкой ошибок.

Purpose: Пользователь может войти и зарегистрироваться — без этого недоступна учёба.
Output: Полностью рабочие /login и /register с формами, ошибками и редиректом.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-frontend/05-01-SUMMARY.md
@frontend/src/contexts/AuthContext.tsx
@frontend/src/api/auth.ts
@frontend/src/App.tsx

**UI дизайн:**
- Простой, минималистичный: белая карточка по центру страницы
- Цвета: зелёный акцент (#059669 emerald-600) — тематика Корана
- Tailwind классы: форма в белом блоке, shadow-md, rounded-xl
- Обработка ошибок: красный текст под полем или под формой
- Loading state: кнопка disabled + "Загрузка..."
</context>

<tasks>

<task type="auto">
  <name>Task 1: LoginPage — форма входа</name>
  <files>frontend/src/pages/LoginPage.tsx</files>
  <action>
Создать директорию frontend/src/pages/.

Создать frontend/src/pages/LoginPage.tsx:
```typescript
import { useState, FormEvent } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { authApi } from '../api/auth'
import { useAuth } from '../contexts/AuthContext'

export default function LoginPage() {
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [error, setError] = useState('')
  const [isLoading, setIsLoading] = useState(false)
  const { login } = useAuth()
  const navigate = useNavigate()

  const handleSubmit = async (e: FormEvent) => {
    e.preventDefault()
    setError('')
    setIsLoading(true)

    try {
      const res = await authApi.login(email, password)
      await login(res.data.access_token)
      navigate('/dashboard')
    } catch (err: any) {
      const detail = err.response?.data?.detail
      setError(typeof detail === 'string' ? detail : 'Неверный email или пароль')
    } finally {
      setIsLoading(false)
    }
  }

  return (
    <div className="min-h-screen bg-gradient-to-br from-emerald-50 to-teal-100 flex items-center justify-center p-4">
      <div className="bg-white rounded-2xl shadow-lg w-full max-w-md p-8">
        {/* Заголовок */}
        <div className="text-center mb-8">
          <h1 className="text-3xl font-bold text-gray-900 mb-1">Quran Arabic</h1>
          <p className="text-gray-500 text-sm">Изучайте слова Корана</p>
          {/* Арабская надпись — басмала */}
          <p className="arabic-text text-2xl text-emerald-700 mt-3" dir="rtl">
            بِسْمِ اللَّهِ
          </p>
        </div>

        {/* Форма */}
        <form onSubmit={handleSubmit} className="space-y-4">
          <div>
            <label className="block text-sm font-medium text-gray-700 mb-1">
              Email
            </label>
            <input
              type="email"
              value={email}
              onChange={e => setEmail(e.target.value)}
              required
              autoComplete="email"
              className="w-full px-4 py-2.5 border border-gray-300 rounded-lg focus:ring-2 focus:ring-emerald-500 focus:border-transparent outline-none transition"
              placeholder="your@email.com"
            />
          </div>

          <div>
            <label className="block text-sm font-medium text-gray-700 mb-1">
              Пароль
            </label>
            <input
              type="password"
              value={password}
              onChange={e => setPassword(e.target.value)}
              required
              autoComplete="current-password"
              className="w-full px-4 py-2.5 border border-gray-300 rounded-lg focus:ring-2 focus:ring-emerald-500 focus:border-transparent outline-none transition"
              placeholder="••••••••"
            />
          </div>

          {/* Ошибка */}
          {error && (
            <div className="bg-red-50 border border-red-200 text-red-700 px-4 py-2.5 rounded-lg text-sm">
              {error}
            </div>
          )}

          <button
            type="submit"
            disabled={isLoading}
            className="w-full bg-emerald-600 hover:bg-emerald-700 disabled:bg-emerald-400 text-white font-semibold py-3 rounded-lg transition-colors"
          >
            {isLoading ? 'Вход...' : 'Войти'}
          </button>
        </form>

        <p className="text-center text-sm text-gray-600 mt-6">
          Нет аккаунта?{' '}
          <Link to="/register" className="text-emerald-600 hover:text-emerald-700 font-medium">
            Зарегистрироваться
          </Link>
        </p>
      </div>
    </div>
  )
}
```
  </action>
  <verify>cd frontend && npm run build — без ошибок TypeScript; открыть /login — форма отображается корректно</verify>
  <done>LoginPage отображает форму с email + password, кнопка "Войти", ссылка на /register</done>
</task>

<task type="auto">
  <name>Task 2: RegisterPage + обновить App.tsx + Dashboard placeholder</name>
  <files>
    frontend/src/pages/RegisterPage.tsx,
    frontend/src/pages/DashboardPage.tsx,
    frontend/src/App.tsx
  </files>
  <action>
Создать frontend/src/pages/RegisterPage.tsx:
```typescript
import { useState, FormEvent } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { authApi } from '../api/auth'
import { useAuth } from '../contexts/AuthContext'

export default function RegisterPage() {
  const [form, setForm] = useState({ email: '', username: '', password: '' })
  const [error, setError] = useState('')
  const [isLoading, setIsLoading] = useState(false)
  const { login } = useAuth()
  const navigate = useNavigate()

  const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    setForm(prev => ({ ...prev, [e.target.name]: e.target.value }))
  }

  const handleSubmit = async (e: FormEvent) => {
    e.preventDefault()
    setError('')
    setIsLoading(true)
    try {
      await authApi.register(form)
      // Авто-логин после регистрации
      const loginRes = await authApi.login(form.email, form.password)
      await login(loginRes.data.access_token)
      navigate('/dashboard')
    } catch (err: any) {
      const detail = err.response?.data?.detail
      setError(typeof detail === 'string' ? detail : 'Ошибка регистрации')
    } finally {
      setIsLoading(false)
    }
  }

  return (
    <div className="min-h-screen bg-gradient-to-br from-emerald-50 to-teal-100 flex items-center justify-center p-4">
      <div className="bg-white rounded-2xl shadow-lg w-full max-w-md p-8">
        <div className="text-center mb-8">
          <h1 className="text-3xl font-bold text-gray-900 mb-1">Регистрация</h1>
          <p className="text-gray-500 text-sm">Начните изучать арабский через Коран</p>
        </div>

        <form onSubmit={handleSubmit} className="space-y-4">
          {(['email', 'username', 'password'] as const).map(field => (
            <div key={field}>
              <label className="block text-sm font-medium text-gray-700 mb-1 capitalize">
                {field === 'username' ? 'Имя пользователя' : field === 'password' ? 'Пароль' : 'Email'}
              </label>
              <input
                type={field === 'password' ? 'password' : field === 'email' ? 'email' : 'text'}
                name={field}
                value={form[field]}
                onChange={handleChange}
                required
                minLength={field === 'password' ? 8 : undefined}
                className="w-full px-4 py-2.5 border border-gray-300 rounded-lg focus:ring-2 focus:ring-emerald-500 focus:border-transparent outline-none transition"
                placeholder={field === 'email' ? 'your@email.com' : field === 'username' ? 'username123' : '••••••••'}
              />
            </div>
          ))}

          {error && (
            <div className="bg-red-50 border border-red-200 text-red-700 px-4 py-2.5 rounded-lg text-sm">
              {error}
            </div>
          )}

          <button
            type="submit"
            disabled={isLoading}
            className="w-full bg-emerald-600 hover:bg-emerald-700 disabled:bg-emerald-400 text-white font-semibold py-3 rounded-lg transition-colors"
          >
            {isLoading ? 'Регистрация...' : 'Создать аккаунт'}
          </button>
        </form>

        <p className="text-center text-sm text-gray-600 mt-6">
          Уже есть аккаунт?{' '}
          <Link to="/login" className="text-emerald-600 hover:text-emerald-700 font-medium">
            Войти
          </Link>
        </p>
      </div>
    </div>
  )
}
```

Создать frontend/src/pages/DashboardPage.tsx — минимальный placeholder с навбаром:
```typescript
import { useNavigate } from 'react-router-dom'
import { useAuth } from '../contexts/AuthContext'

export default function DashboardPage() {
  const { user, logout } = useAuth()
  const navigate = useNavigate()

  return (
    <div className="min-h-screen bg-gray-50">
      {/* Навбар */}
      <nav className="bg-white border-b border-gray-200 px-4 py-3">
        <div className="max-w-4xl mx-auto flex items-center justify-between">
          <div className="flex items-center gap-2">
            <span className="text-emerald-600 font-bold text-lg">Quran Arabic</span>
            <span className="arabic-text text-emerald-700" dir="rtl">القرآن</span>
          </div>
          <div className="flex items-center gap-3">
            <span className="text-sm text-gray-600">{user?.username}</span>
            <button
              onClick={logout}
              className="text-sm text-gray-500 hover:text-red-600 transition-colors"
            >
              Выйти
            </button>
          </div>
        </div>
      </nav>

      {/* Контент */}
      <main className="max-w-4xl mx-auto p-6">
        <h2 className="text-2xl font-bold text-gray-900 mb-6">Добро пожаловать, {user?.username}!</h2>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-100">
            <h3 className="font-semibold text-gray-900 mb-2">📚 Начать занятие</h3>
            <p className="text-gray-500 text-sm mb-4">Повторите слова 30-го джуза</p>
            <button
              onClick={() => navigate('/study')}
              className="w-full bg-emerald-600 hover:bg-emerald-700 text-white font-medium py-2.5 rounded-lg transition-colors"
            >
              Учиться
            </button>
          </div>
          <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-100">
            <h3 className="font-semibold text-gray-900 mb-2">📊 Статистика</h3>
            <p className="text-gray-500 text-sm">Будет доступна в следующей фазе</p>
          </div>
        </div>
      </main>
    </div>
  )
}
```

Обновить frontend/src/App.tsx — заменить placeholder импорты на реальные страницы:
```typescript
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom'
import { AuthProvider, useAuth } from './contexts/AuthContext'
import LoginPage from './pages/LoginPage'
import RegisterPage from './pages/RegisterPage'
import DashboardPage from './pages/DashboardPage'

const StudyPage = () => <div className="p-8 text-center">Study (coming in plan 05-04)</div>

function ProtectedRoute({ children }: { children: React.ReactNode }) {
  const { isAuthenticated, isLoading } = useAuth()
  if (isLoading) return (
    <div className="flex items-center justify-center h-screen">
      <div className="text-emerald-600 text-lg">Загрузка...</div>
    </div>
  )
  if (!isAuthenticated) return <Navigate to="/login" replace />
  return <>{children}</>
}

function AppRoutes() {
  const { isAuthenticated } = useAuth()
  return (
    <Routes>
      <Route path="/login" element={isAuthenticated ? <Navigate to="/dashboard" /> : <LoginPage />} />
      <Route path="/register" element={isAuthenticated ? <Navigate to="/dashboard" /> : <RegisterPage />} />
      <Route path="/dashboard" element={<ProtectedRoute><DashboardPage /></ProtectedRoute>} />
      <Route path="/study" element={<ProtectedRoute><StudyPage /></ProtectedRoute>} />
      <Route path="/" element={<Navigate to={isAuthenticated ? "/dashboard" : "/login"} />} />
    </Routes>
  )
}

export default function App() {
  return (
    <BrowserRouter>
      <AuthProvider>
        <div className="min-h-screen bg-gray-50">
          <AppRoutes />
        </div>
      </AuthProvider>
    </BrowserRouter>
  )
}
```
  </action>
  <verify>npm run build проходит без ошибок TypeScript; dev сервер запущен — страницы /login и /register отображаются</verify>
  <done>Login и Register страницы работают, DashboardPage показывает навбар с именем пользователя и кнопкой выхода</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Страницы Login и Register с формами, валидацией и навигацией.</what-built>
  <how-to-verify>
    1. Убедись что backend запущен: cd backend && uvicorn app.main:app --reload
    2. Запусти frontend: cd frontend && npm run dev
    3. Открой http://localhost:5173 — должен редиректить на /login
    4. Зарегистрируйся: email, username (3+ chars), password (8+ chars)
       - Ожидаем: редирект на /dashboard с приветствием
    5. Нажми "Выйти" — редирект на /login
    6. Войди с теми же данными — опять /dashboard
    7. Попробуй неверный пароль — красное сообщение об ошибке
    8. Проверь /register — ссылка "Войти" работает
    9. Арабский текст "بِسْمِ اللَّهِ" на /login отображается шрифтом Amiri
  </how-to-verify>
  <resume-signal>Напиши "approved" если auth flow работает, или опиши проблемы</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build — без ошибок
- [ ] /login отображает форму с арабским текстом шрифтом Amiri
- [ ] Регистрация → авто-логин → /dashboard
- [ ] Неверный пароль → красная ошибка
- [ ] Logout → /login
- [ ] /dashboard → кнопка "Учиться" → /study
</verification>

<success_criteria>
- Auth flow полностью работает через реальный API
- Ошибки отображаются из API response
- AuthContext сохраняет токен в localStorage
- Навбар показывает username
- Logout очищает токен
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend/05-02-SUMMARY.md`:

# Phase 5 Plan 02: Auth Pages Summary

**[Substantive one-liner]**

## Accomplishments

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Step

Ready for 05-03-PLAN.md
</output>
