---
phase: 02-data-pipeline
plan: 02
type: execute
depends_on: ["02-01"]
files_modified:
  - backend/app/utils/__init__.py
  - backend/app/utils/arabic.py
  - backend/app/data/fetcher.py
  - backend/tests/test_arabic_utils.py
---

<objective>
Написать fetcher для alquran.cloud API и утилиты нормализации арабского текста.

Purpose: Получить сырые данные 30-го джуза и подготовить логику обработки арабских слов.
Output: QuranFetcher класс (HTTP → dict) + normalize_arabic функция (TDD).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-data-pipeline/02-01-SUMMARY.md
@backend/app/models/ayah.py
@backend/app/models/word.py

**Важный контекст — alquran.cloud API:**

Endpoint для 30-го джуза с двумя изданиями сразу:
GET https://api.alquran.cloud/v1/juz/30/editions/quran-simple,ru.kuliev

Ответ — массив из 2 объектов:
```json
[
  {
    "number": 30,
    "edition": {"identifier": "quran-simple", "language": "ar"},
    "ayahs": [
      {
        "number": 5673,
        "text": "عَمَّ يَتَسَاءَلُونَ",
        "surah": {
          "number": 78,
          "name": "سُورَةُ النَّبَإِ",
          "englishName": "An-Naba",
          "englishNameTranslation": "The Tidings",
          "numberOfAyahs": 40,
          "revelationType": "Meccan"
        },
        "numberInSurah": 1
      }
    ]
  },
  {
    "number": 30,
    "edition": {"identifier": "ru.kuliev", "language": "ru"},
    "ayahs": [
      {
        "number": 5673,
        "text": "О чём они вопрошают друг друга?",
        "numberInSurah": 1
      }
    ]
  }
]
```

Арабская нормализация — Unicode диапазоны огласовок:
- Харакат (огласовки): U+064B–U+065F
- Shadda: U+0651
- Superscript Alef: U+0670
- Таш'кил: U+06D6–U+06DC, U+06DF–U+06E4, U+06E7, U+06E8, U+06EA–U+06ED
- Нормализация алеф: آ(U+0622) → ا, أ(U+0623) → ا, إ(U+0625) → ا
- Удалить татвиль (кашида): U+0640
</context>

<tasks>

<task type="auto">
  <name>Task 1: Arabic normalization utils + тесты (TDD)</name>
  <files>
    backend/app/utils/__init__.py,
    backend/app/utils/arabic.py,
    backend/tests/test_arabic_utils.py
  </files>
  <action>
Создать backend/app/utils/arabic.py — функции для работы с арабским текстом:
```python
import re

# Диапазоны Unicode для огласовок Корана (харакат/ташкиль)
ARABIC_DIACRITICS = re.compile(
    r'[\u064B-\u065F'     # Харакат: фатхатан, думматан, касратан, фатха, думма, касра, шадда, сукун и т.д.
    r'\u0670'             # Superscript Alef (алеф хинджер)
    r'\u06D6-\u06DC'      # Знаки тилавы Корана
    r'\u06DF-\u06E4'      # Знаки тилавы Корана
    r'\u06E7\u06E8'       # Знаки тилавы Корана
    r'\u06EA-\u06ED]'     # Знаки тилавы Корана
)

# Формы алефа → базовый алеф
ALEF_VARIANTS = str.maketrans({
    '\u0622': '\u0627',  # آ → ا (Alef with Madda)
    '\u0623': '\u0627',  # أ → ا (Alef with Hamza Above)
    '\u0625': '\u0627',  # إ → ا (Alef with Hamza Below)
    '\u0671': '\u0627',  # ٱ → ا (Alef Wasla)
})

TATWEEL = '\u0640'  # Татвиль (кашида) ـ


def normalize_arabic(text: str) -> str:
    """
    Нормализовать арабское слово для дедупликации:
    1. Удалить огласовки (харакат)
    2. Нормализовать формы алефа
    3. Удалить татвиль (кашида)
    4. Убрать лишние пробелы
    """
    text = ARABIC_DIACRITICS.sub('', text)
    text = text.translate(ALEF_VARIANTS)
    text = text.replace(TATWEEL, '')
    return text.strip()


def extract_words(arabic_text: str) -> list[str]:
    """
    Извлечь список слов из арабского текста аята.
    Возвращает слова С огласовками (для отображения).
    """
    # Разбить по пробелам, убрать пустые строки
    words = [w.strip() for w in arabic_text.split() if w.strip()]
    # Убрать слова-знаки препинания (если есть)
    words = [w for w in words if any('\u0600' <= c <= '\u06FF' for c in w)]
    return words
```

Создать backend/tests/test_arabic_utils.py:
```python
import pytest
from app.utils.arabic import normalize_arabic, extract_words


class TestNormalizeArabic:
    def test_removes_fatha(self):
        # عَمَّ → عم (убирает фатху и шадду)
        assert normalize_arabic("عَمَّ") == "عم"

    def test_removes_sukun(self):
        assert normalize_arabic("مِنْ") == "من"

    def test_normalizes_alef_with_hamza(self):
        # أ → ا
        assert normalize_arabic("أَنَا") == "انا"

    def test_normalizes_alef_with_madda(self):
        # آ → ا
        assert normalize_arabic("آمَنَ") == "امن"

    def test_removes_tatweel(self):
        assert normalize_arabic("اللـه") == "الله"

    def test_plain_word_unchanged(self):
        # Слово без огласовок не меняется
        assert normalize_arabic("الله") == "الله"

    def test_empty_string(self):
        assert normalize_arabic("") == ""

    def test_strips_whitespace(self):
        assert normalize_arabic("  عَمَّ  ") == "عم"


class TestExtractWords:
    def test_splits_ayah_into_words(self):
        ayah = "عَمَّ يَتَسَاءَلُونَ"
        words = extract_words(ayah)
        assert len(words) == 2
        assert words[0] == "عَمَّ"
        assert words[1] == "يَتَسَاءَلُونَ"

    def test_empty_text(self):
        assert extract_words("") == []

    def test_single_word(self):
        assert extract_words("الله") == ["الله"]
```

Запустить тесты:
```bash
cd backend
source .venv/bin/activate
pytest tests/test_arabic_utils.py -v
```
  </action>
  <verify>pytest tests/test_arabic_utils.py -v — все тесты GREEN (11 тестов)</verify>
  <done>Все тесты проходят, normalize_arabic и extract_words работают корректно</done>
</task>

<task type="auto">
  <name>Task 2: QuranFetcher — загрузка данных из alquran.cloud</name>
  <files>
    backend/app/data/__init__.py,
    backend/app/data/fetcher.py
  </files>
  <action>
Создать директорию backend/app/data/ с __init__.py.

Создать backend/app/data/fetcher.py:
```python
"""
Загрузчик данных 30-го джуза из alquran.cloud API.

API документация: https://alquran.cloud/api
Используем endpoint: GET /v1/juz/{juz}/editions/{edition1},{edition2}
"""
import httpx
from dataclasses import dataclass
from typing import Optional
import time


BASE_URL = "https://api.alquran.cloud/v1"
JUZ_NUMBER = 30
# quran-simple = арабский текст без огласовок (базовый)
# quran-uthmani = арабский текст с огласовками (для отображения)
# ru.kuliev = русский перевод Кулиева
EDITIONS = "quran-uthmani,ru.kuliev"


@dataclass
class AyahData:
    global_number: int
    surah_number: int
    ayah_number: int
    arabic_text: str      # с огласовками
    russian_translation: Optional[str]
    surah_name_arabic: str
    surah_name_english: str
    surah_name_transliteration: str
    surah_revelation_type: str
    surah_total_ayahs: int


class QuranFetcher:
    """Загружает данные Корана из alquran.cloud API."""

    def __init__(self, timeout: float = 30.0):
        self.timeout = timeout

    def fetch_juz(self, juz_number: int = JUZ_NUMBER) -> list[AyahData]:
        """
        Загрузить все аяты джуза с арабским текстом и русским переводом.

        Returns: список AyahData, отсортированный по global_number.
        Raises: httpx.HTTPError при ошибке сети.
        """
        url = f"{BASE_URL}/juz/{juz_number}/editions/{EDITIONS}"

        print(f"Загружаем джуз {juz_number} из {url}...")

        with httpx.Client(timeout=self.timeout) as client:
            response = client.get(url)
            response.raise_for_status()
            data = response.json()

        # data["data"] — список из 2 элементов: [арабский, русский]
        editions_data = data["data"]
        arabic_data = next(e for e in editions_data if "uthmani" in e["edition"]["identifier"])
        russian_data = next(e for e in editions_data if e["edition"]["identifier"] == "ru.kuliev")

        # Создаём словарь русского перевода: global_number → text
        russian_by_number = {
            ayah["number"]: ayah["text"]
            for ayah in russian_data["ayahs"]
        }

        ayahs = []
        for ayah in arabic_data["ayahs"]:
            surah = ayah["surah"]
            ayahs.append(AyahData(
                global_number=ayah["number"],
                surah_number=surah["number"],
                ayah_number=ayah["numberInSurah"],
                arabic_text=ayah["text"],
                russian_translation=russian_by_number.get(ayah["number"]),
                surah_name_arabic=surah["name"],
                surah_name_english=surah["englishName"],
                surah_name_transliteration=surah.get("englishNameTranslation", ""),
                surah_revelation_type=surah.get("revelationType", ""),
                surah_total_ayahs=surah.get("numberOfAyahs", 0),
            ))

        print(f"Загружено {len(ayahs)} аятов из {len(set(a.surah_number for a in ayahs))} сур")
        return sorted(ayahs, key=lambda a: a.global_number)
```

Проверить что fetcher работает (нужен интернет):
```bash
cd backend
source .venv/bin/activate
python -c "
from app.data.fetcher import QuranFetcher
f = QuranFetcher()
ayahs = f.fetch_juz(30)
print(f'Первый аят: {ayahs[0].arabic_text}')
print(f'Перевод: {ayahs[0].russian_translation}')
print(f'Всего аятов: {len(ayahs)}')
"
```

Ожидаемый вывод:
```
Загружено 564 аятов из 37 сур
Первый аят: عَمَّ يَتَسَاءَلُونَ
Перевод: О чём они вопрошают друг друга?
Всего аятов: 564
```
  </action>
  <verify>python -c "from app.data.fetcher import QuranFetcher; f = QuranFetcher(); ayahs = f.fetch_juz(30); assert len(ayahs) > 500, f'Got {len(ayahs)}'; print('OK')"</verify>
  <done>QuranFetcher загружает 500+ аятов 30-го джуза, первый аят содержит арабский текст и русский перевод</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest tests/test_arabic_utils.py -v — все тесты GREEN
- [ ] python -c import QuranFetcher — без ошибок
- [ ] QuranFetcher().fetch_juz(30) возвращает 500+ аятов
- [ ] Первый аят: arabic_text = "عَمَّ يَتَسَاءَلُونَ", russian_translation не None
</verification>

<success_criteria>
- normalize_arabic удаляет огласовки и нормализует алеф (подтверждено тестами)
- extract_words разбивает аят на слова
- QuranFetcher загружает реальные данные из alquran.cloud API
- 11 тестов проходят
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-pipeline/02-02-SUMMARY.md`:

# Phase 2 Plan 02: Fetcher & Arabic Utils Summary

**[Substantive one-liner]**

## Accomplishments

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Step

Ready for 02-03-PLAN.md
</output>
