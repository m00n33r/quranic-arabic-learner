---
phase: 01-foundation
plan: 03
type: execute
depends_on: ["01-01", "01-02"]
files_modified:
  - backend/app/core/database.py
  - backend/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/versions/
---

<objective>
Настроить SQLAlchemy + Alembic и запустить полный стек через Docker Compose.

Purpose: База данных подключена, ORM работает, миграции применяются — фундамент для всех моделей данных.
Output: database.py с сессией, Alembic инициализирован, docker-compose up поднимает db + backend.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SQLAlchemy database.py — движок и сессии</name>
  <files>
    backend/app/core/database.py,
    backend/app/models/__init__.py,
    backend/app/models/base.py
  </files>
  <action>
Создать backend/app/core/database.py:
```python
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker, Session
from typing import Generator

from app.core.config import settings

engine = create_engine(
    settings.database_url,
    pool_pre_ping=True,  # Проверяет соединение перед использованием
    pool_size=10,
    max_overflow=20,
)

SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)


def get_db() -> Generator[Session, None, None]:
    """FastAPI dependency для получения сессии БД."""
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

Создать backend/app/models/base.py — базовый класс для всех моделей:
```python
from sqlalchemy.orm import DeclarativeBase
from sqlalchemy import Column, Integer, DateTime, func


class Base(DeclarativeBase):
    pass


class TimestampMixin:
    """Миксин добавляет created_at и updated_at."""
    created_at = Column(DateTime(timezone=True), server_default=func.now(), nullable=False)
    updated_at = Column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now(), nullable=False)
```

Обновить backend/app/models/__init__.py:
```python
from app.models.base import Base, TimestampMixin

__all__ = ["Base", "TimestampMixin"]
```

Добавить в backend/app/main.py импорт для проверки соединения с БД:
```python
# Добавить endpoint для проверки БД
from sqlalchemy import text
from app.core.database import get_db
from fastapi import Depends
from sqlalchemy.orm import Session

@app.get("/health/db")
async def db_health(db: Session = Depends(get_db)):
    db.execute(text("SELECT 1"))
    return {"database": "ok"}
```
  </action>
  <verify>cd backend && source .venv/bin/activate && python -c "from app.core.database import engine, get_db; print('DB module OK')"</verify>
  <done>database.py импортируется без ошибок, get_db функция существует, Base класс доступен</done>
</task>

<task type="auto">
  <name>Task 2: Alembic инициализация и конфигурация</name>
  <files>
    backend/alembic.ini,
    backend/alembic/env.py,
    backend/alembic/script.py.mako
  </files>
  <action>
Инициализировать Alembic в директории backend/:
```bash
cd backend
source .venv/bin/activate
alembic init alembic
```

Обновить backend/alembic.ini — изменить строку подключения на placeholder (реальная берётся из env.py):
```ini
# Найти строку sqlalchemy.url = и заменить на:
sqlalchemy.url = postgresql://quran:quran@localhost:5432/quran_learner
```

Обновить backend/alembic/env.py для использования наших моделей и settings:
```python
from logging.config import fileConfig
from sqlalchemy import engine_from_config, pool
from alembic import context
import sys
import os

# Добавляем backend/ в sys.path для импорта app
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from app.core.config import settings
from app.models.base import Base
# Импортируем все модели, чтобы Alembic их видел (пока пустые)
import app.models  # noqa: F401

config = context.config

# Переопределяем URL из settings (не из alembic.ini)
config.set_main_option("sqlalchemy.url", settings.database_url)

if config.config_file_name is not None:
    fileConfig(config.config_file_name)

target_metadata = Base.metadata


def run_migrations_offline() -> None:
    url = config.get_main_option("sqlalchemy.url")
    context.configure(
        url=url,
        target_metadata=target_metadata,
        literal_binds=True,
        dialect_opts={"paramstyle": "named"},
    )
    with context.begin_transaction():
        context.run_migrations()


def run_migrations_online() -> None:
    connectable = engine_from_config(
        config.get_section(config.config_ini_section, {}),
        prefix="sqlalchemy.",
        poolclass=pool.NullPool,
    )
    with connectable.connect() as connection:
        context.configure(connection=connection, target_metadata=target_metadata)
        with context.begin_transaction():
            context.run_migrations()


if context.is_offline_mode():
    run_migrations_offline()
else:
    run_migrations_online()
```

Проверить что alembic видит конфигурацию:
```bash
cd backend
source .venv/bin/activate
alembic current  # Должен показать "No current revision"
```
  </action>
  <verify>cd backend && source .venv/bin/activate && alembic check 2>&1 | grep -v "ERROR" || alembic current 2>&1</verify>
  <done>alembic.ini создан, env.py настроен с нашими settings и Base.metadata, директория alembic/versions/ существует</done>
</task>

<task type="auto">
  <name>Task 3: Docker Compose — поднять полный стек и проверить</name>
  <files>
    (нет новых файлов — только запуск и проверка)
  </files>
  <action>
Запустить полный стек через Docker Compose:
```bash
cd /Users/mun/KFU_studies/course_project
docker compose up -d --build
```

Подождать пока db будет healthy (healthcheck в docker-compose.yml).

Применить первую "пустую" миграцию внутри контейнера (инициализация alembic_version таблицы):
```bash
docker compose exec backend alembic upgrade head
```

Если БД ещё не запущена локально, можно запустить только db сервис и применить миграции локально:
```bash
docker compose up -d db
# Подождать 5 секунд
sleep 5
cd backend && source .venv/bin/activate && alembic upgrade head
```

Проверить endpoints:
```bash
curl http://localhost:8000/health
curl http://localhost:8000/health/db
```
  </action>
  <verify>curl http://localhost:8000/health/db возвращает {"database":"ok"}; docker compose ps показывает db и backend в состоянии running/healthy</verify>
  <done>Оба контейнера запущены, /health/db возвращает 200, alembic_version таблица создана в БД</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Полный стек: FastAPI + PostgreSQL через Docker Compose. Alembic настроен.</what-built>
  <how-to-verify>
    1. Убедись что docker compose up запущен
    2. Открой http://localhost:8000/docs — должен открыться Swagger UI
    3. Проверь GET /health → {"status":"ok","environment":"development"}
    4. Проверь GET /health/db → {"database":"ok"}
    5. Запусти: docker compose ps — оба сервиса должны быть Up
    6. Запусти: cd backend && source .venv/bin/activate && alembic current — должно показать revision без ошибок
  </how-to-verify>
  <resume-signal>Напиши "approved" если всё работает, или опиши ошибки для исправления</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] docker compose up -d --build проходит без ошибок
- [ ] curl http://localhost:8000/health → {"status":"ok"}
- [ ] curl http://localhost:8000/health/db → {"database":"ok"}
- [ ] alembic current не выдаёт ошибок
- [ ] docker compose ps показывает оба сервиса как running
</verification>

<success_criteria>
- SQLAlchemy engine подключается к PostgreSQL
- Alembic настроен и читает DATABASE_URL из settings
- Base.metadata доступна для автогенерации миграций
- Docker Compose поднимает db + backend
- /health/db возвращает 200
- Фаза 1 завершена, готово к Phase 2 (Data Pipeline)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`:

# Phase 1 Plan 03: Database & Alembic Summary

**[Substantive one-liner]**

## Accomplishments

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Step

Phase 1 complete — ready for Phase 2: Data Pipeline
</output>
