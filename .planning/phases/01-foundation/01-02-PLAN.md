---
phase: 01-foundation
plan: 02
type: execute
depends_on: ["01-01"]
files_modified:
  - backend/app/main.py
  - backend/app/core/config.py
  - backend/app/core/database.py
  - backend/Dockerfile
---

<objective>
Создать рабочее FastAPI приложение с настройками, CORS и health check.

Purpose: Основа бэкенда — приложение должно запускаться, отвечать на запросы и быть готово к добавлению роутеров.
Output: FastAPI app с pydantic-settings конфигурацией, CORS для React dev-server, health endpoint, Dockerfile.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FastAPI приложение — конфигурация, CORS, health check</name>
  <files>
    backend/app/core/config.py,
    backend/app/main.py
  </files>
  <action>
Создать backend/app/core/config.py — настройки через pydantic-settings:
```python
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8")

    # Database
    database_url: str = "postgresql://quran:quran@localhost:5432/quran_learner"

    # Auth
    secret_key: str = "dev-secret-key-change-in-prod"
    algorithm: str = "HS256"
    access_token_expire_minutes: int = 60 * 24  # 24 hours

    # App
    environment: str = "development"
    allowed_origins: list[str] = [
        "http://localhost:5173",  # Vite dev server
        "http://localhost:3000",
    ]


settings = Settings()
```

Создать backend/app/main.py:
```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

from app.core.config import settings

app = FastAPI(
    title="Quran Arabic Learner API",
    description="API для изучения слов 30-го джуза Корана",
    version="0.1.0",
)

app.add_middleware(
    CORSMiddleware,
    allow_origins=settings.allowed_origins,
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)


@app.get("/health")
async def health_check():
    return {"status": "ok", "environment": settings.environment}


@app.get("/")
async def root():
    return {"message": "Quran Arabic Learner API"}
```

Запустить для проверки:
```bash
cd backend
source .venv/bin/activate
uvicorn app.main:app --reload --port 8000
```
  </action>
  <verify>curl http://localhost:8000/health возвращает {"status":"ok","environment":"development"}</verify>
  <done>FastAPI запускается, /health возвращает 200, /docs открывает Swagger UI</done>
</task>

<task type="auto">
  <name>Task 2: Dockerfile для бэкенда</name>
  <files>backend/Dockerfile</files>
  <action>
Создать backend/Dockerfile — multi-stage не нужен для разработки, используем простой образ:

```dockerfile
FROM python:3.11-slim

WORKDIR /app

# Системные зависимости для psycopg2
RUN apt-get update && apt-get install -y \
    libpq-dev \
    gcc \
    && rm -rf /var/lib/apt/lists/*

# Устанавливаем зависимости отдельно (кэш Docker)
COPY pyproject.toml .
RUN pip install --no-cache-dir -e .

# Копируем код
COPY . .

EXPOSE 8000

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

ВАЖНО: В docker-compose.yml уже настроен --reload через command override для dev.
В Dockerfile CMD без --reload — это production команда.

Проверить сборку образа:
```bash
cd backend
docker build -t quran-backend .
```
  </action>
  <verify>docker build -t quran-backend ./backend выходит с кодом 0; docker images | grep quran-backend</verify>
  <done>Docker образ собирается без ошибок</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] uvicorn app.main:app --reload запускается без ошибок
- [ ] curl http://localhost:8000/health → {"status":"ok"}
- [ ] curl http://localhost:8000/docs → 200 (Swagger UI)
- [ ] docker build ./backend завершается успешно
</verification>

<success_criteria>
- FastAPI приложение запускается локально
- CORS настроен для http://localhost:5173
- /health endpoint возвращает 200
- Dockerfile собирает образ
- pydantic-settings читает .env файл
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`:

# Phase 1 Plan 02: FastAPI Application Summary

**[Substantive one-liner]**

## Accomplishments

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Step

Ready for 01-03-PLAN.md
</output>
