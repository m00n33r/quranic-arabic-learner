---
phase: 01-foundation
plan: 01
type: execute
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - .gitignore
  - backend/pyproject.toml
  - backend/.dockerignore
---

<objective>
Создать структуру монорепо и настроить зависимости бэкенда.

Purpose: Фундамент для всех последующих фаз — правильная структура директорий и зафиксированные зависимости Python.
Output: Монорепо с backend/ и frontend/, docker-compose.yml, pyproject.toml с зависимостями.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать структуру монорепо</name>
  <files>
    docker-compose.yml,
    .env.example,
    .gitignore,
    backend/.dockerignore,
    frontend/.gitkeep
  </files>
  <action>
Создать следующую структуру директорий:

```
course_project/
├── backend/
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py          (пустой пока)
│   │   ├── core/
│   │   │   └── __init__.py
│   │   ├── models/
│   │   │   └── __init__.py
│   │   └── api/
│   │       └── __init__.py
│   └── tests/
│       └── __init__.py
├── frontend/               (пустая папка с .gitkeep)
├── docker-compose.yml
├── .env.example
└── .gitignore
```

docker-compose.yml:
```yaml
version: "3.9"

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-quran}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-quran}
      POSTGRES_DB: ${POSTGRES_DB:-quran_learner}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-quran}"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-quran}:${POSTGRES_PASSWORD:-quran}@db:5432/${POSTGRES_DB:-quran_learner}
      SECRET_KEY: ${SECRET_KEY:-dev-secret-key-change-in-prod}
      ENVIRONMENT: ${ENVIRONMENT:-development}
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./backend:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

volumes:
  postgres_data:
```

.env.example:
```
POSTGRES_USER=quran
POSTGRES_PASSWORD=quran
POSTGRES_DB=quran_learner
SECRET_KEY=your-secret-key-here-change-in-production
ENVIRONMENT=development
```

.gitignore — стандартный Python + Node:
```
# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
.env
*.egg-info/

# Node
node_modules/
dist/
.next/

# Docker
.docker/

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
```

backend/.dockerignore:
```
.venv
__pycache__
*.pyc
.env
.git
```

Также создать .env из .env.example для локальной разработки.
  </action>
  <verify>ls -la показывает backend/, frontend/, docker-compose.yml, .env.example, .gitignore; ls backend/app/ показывает __init__.py, main.py, core/, models/, api/</verify>
  <done>Все директории и файлы созданы, структура монорепо готова</done>
</task>

<task type="auto">
  <name>Task 2: Настроить pyproject.toml с зависимостями бэкенда</name>
  <files>backend/pyproject.toml</files>
  <action>
Создать backend/pyproject.toml:

```toml
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "quran-arabic-learner-backend"
version = "0.1.0"
description = "Backend for Quran Arabic Learner"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn[standard]>=0.30.0",
    "sqlalchemy>=2.0.0",
    "alembic>=1.13.0",
    "psycopg2-binary>=2.9.9",
    "pydantic-settings>=2.3.0",
    "python-jose[cryptography]>=3.3.0",
    "passlib[bcrypt]>=1.7.4",
    "python-multipart>=0.0.9",
    "httpx>=0.27.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.2.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=5.0.0",
    "httpx>=0.27.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
```

Затем создать виртуальное окружение и установить зависимости:
```bash
cd backend
python3 -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
```

ВАЖНО: .venv должен быть в backend/.venv (уже в .gitignore).
  </action>
  <verify>cd backend && source .venv/bin/activate && python -c "import fastapi, sqlalchemy, alembic, jose, passlib; print('OK')"</verify>
  <done>Все зависимости установлены, python -c import проходит без ошибок</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ls backend/app/ показывает core/, models/, api/, __init__.py, main.py
- [ ] docker-compose.yml существует и валиден: docker compose config
- [ ] .env создан из .env.example
- [ ] python import fastapi sqlalchemy alembic jose passlib проходит
</verification>

<success_criteria>
- Структура монорепо создана согласно схеме
- docker-compose.yml описывает сервисы db и backend
- Все Python зависимости установлены в backend/.venv
- .gitignore корректно игнорирует .venv, .env, __pycache__
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`:

# Phase 1 Plan 01: Monorepo Structure Summary

**[Substantive one-liner]**

## Accomplishments

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Step

Ready for 01-02-PLAN.md
</output>
