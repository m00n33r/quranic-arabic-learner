---
phase: 08-reader
plan: 03
type: execute
depends_on: ["08-02"]
files_modified:
  - frontend/src/utils/tajweed.ts
  - frontend/src/api/reader.ts
  - frontend/src/pages/QuranReaderPage.tsx
  - frontend/src/App.tsx
  - frontend/src/pages/DashboardPage.tsx
---

<objective>
Создать полноценную страницу чтения Корана: аяты с таджвид-подсветкой, кликабельные слова для добавления в изучение, удобная навигация на мобильном и десктопе.

Purpose: Пользователь читает аяты 30-го джуза, видит таджвид-расцветку, кликает незнакомые слова и они автоматически добавляются в флэшкарточки.
Output: Страница /reader полностью функциональна.
</objective>

<context>
Backend API (из 08-02):
- GET /api/v1/reader/surahs → SurahInfo[]
- GET /api/v1/reader/ayahs/{surah_number} → AyahWithWords[]
  - AyahWithWords.words: WordInAyah[] { word_id: int|null, position, arabic, is_in_study }
- POST /api/v1/reader/enqueue { word_ids: int[] } → { added, already_studying }
- DELETE /api/v1/reader/words/{word_id} → 204

UX флоу:
1. Пользователь выбирает суру (dropdown сверху)
2. Видит аяты один за другим (текущий выделен)
3. Арабский текст — каждое слово кликабельно:
   - Обычное слово: цвет таджвида
   - Выбранное для изучения: жёлтый фон (pending, ещё не добавлено в очередь)
   - Уже в изучении (is_in_study): зелёный фон + иконка ✓
   - Клик на выбранное (pending) → убрать выбор
   - Клик на is_in_study → вызвать DELETE, убрать из is_in_study
4. Перевод аята под арабским текстом
5. Навигация: при переходе на следующий аят → POST /enqueue с pending словами

НАВИГАЦИЯ (обязательно удобная на обоих платформах):
Desktop:
  - Кнопки "← Предыдущий" и "Следующий →" снизу
  - Клавиши: ArrowRight / L / Space = следующий аят
              ArrowLeft / H = предыдущий аят
  - PageDown / PageUp тоже

Mobile:
  - Большие кнопки-области снизу (min-height 64px) с большими иконками < >
  - Swipe up/down для навигации (опционально, если есть время)
  - Кнопки positioned fixed внизу чтобы всегда быть доступны

ТАДЖВИД расцветка (function getTajweedClass(word: string) → CSS-класс):
Цвета:
- Красный (#dc2626): буквы калькаля (ق ط ب ج د) — особенно с суkunом ْ или в конце слова
- Зелёный (#16a34a): ن или م с шаддой ّ (гунна)
- Фиолетовый (#9333ea): любая буква с шаддой ّ
- Оранжевый (#ea580c): танвин (ً ٍ ٌ) — знаки на конце
- Синий (#2563eb): слова начинающиеся с ال (артикль + солнечная/лунная буква)
- Тёмный (#111827): всё остальное

Легенда таджвида: небольшой раскрывающийся блок под навигацией с объяснением цветов.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Утилита tajweed.ts</name>
  <files>frontend/src/utils/tajweed.ts</files>
  <action>
Создать frontend/src/utils/tajweed.ts:
```typescript
// Таджвид-расцветка арабского текста
// Приближённая реализация на уровне слов

export type TajweedRule = 'qalqalah' | 'ghunnah' | 'shadda' | 'tanwin' | 'idgham' | 'none'

const QALQALAH_LETTERS = new Set(['ق', 'ط', 'ب', 'ج', 'د'])
const SUKUN = '\u0652'       // ْ
const SHADDA = '\u0651'      // ّ
const TANWIN = /[\u064B\u064C\u064D]/ // ً ٍ ٌ

// Убрать огласовки для анализа чистых букв
function stripDiacritics(text: string): string {
  return text.replace(/[\u064B-\u065F\u0670\u06D6-\u06DC\u06DF-\u06E4\u06E7\u06E8\u06EA-\u06ED]/g, '')
}

export function getTajweedRule(word: string): TajweedRule {
  const hasShadda = word.includes(SHADDA)
  const hasSukun = word.includes(SUKUN)
  const hasTanwin = TANWIN.test(word)
  const letters = stripDiacritics(word)

  // Гунна: ن или م с шаддой
  if (hasShadda && /[نم]/.test(letters)) return 'ghunnah'

  // Калькаля: буква ق ط ب ج د + сукун, или в конце слова
  for (let i = 0; i < word.length; i++) {
    const ch = word[i]
    if (!QALQALAH_LETTERS.has(ch)) continue
    const next = word[i + 1]
    // Сукун после буквы или буква последняя в слове (после удаления огласовок)
    if (next === SUKUN || i === word.length - 1 || (next && /[\u064B-\u065F]/.test(next) && i === word.length - 2)) {
      return 'qalqalah'
    }
  }
  // Также: буква калькаля последняя в слове (без огласовки)
  if (letters.length > 0 && QALQALAH_LETTERS.has(letters[letters.length - 1])) {
    return 'qalqalah'
  }

  // Шадда (любая другая буква)
  if (hasShadda) return 'shadda'

  // Танвин
  if (hasTanwin) return 'tanwin'

  return 'none'
}

export const TAJWEED_CLASSES: Record<TajweedRule, string> = {
  qalqalah: 'text-red-600 font-medium',
  ghunnah:  'text-green-600 font-medium',
  shadda:   'text-purple-600',
  tanwin:   'text-orange-500',
  idgham:   'text-blue-600',
  none:     'text-gray-900',
}

export const TAJWEED_LEGEND: Array<{ rule: TajweedRule; label: string; description: string }> = [
  { rule: 'qalqalah', label: 'Калькаля',  description: 'Буквы ق ط ب ج د — лёгкое отскакивание при сукуне' },
  { rule: 'ghunnah',  label: 'Гунна',     description: 'ن или م с шаддой — назализация (2 доли)' },
  { rule: 'shadda',   label: 'Шадда',     description: 'Удвоение любой буквы' },
  { rule: 'tanwin',   label: 'Танвин',    description: 'Удвоённые гласные в конце слова' },
]
```
  </action>
  <verify>cd frontend && npx tsc --noEmit 2>&1 | grep tajweed || echo "tajweed types ok"</verify>
  <done>tajweed.ts создан, типы корректны</done>
</task>

<task type="auto">
  <name>Task 2: API клиент reader.ts</name>
  <files>frontend/src/api/reader.ts</files>
  <action>
Создать frontend/src/api/reader.ts:
```typescript
import { apiClient } from './client'

export interface SurahInfo {
  number: number
  name_arabic: string
  name_english: string
  name_transliteration: string | null
  total_ayahs: number
}

export interface WordInAyah {
  word_id: number | null
  position: number
  arabic: string
  is_in_study: boolean
}

export interface AyahWithWords {
  id: number
  surah_number: number
  ayah_number: number
  arabic_text: string
  russian_translation: string | null
  words: WordInAyah[]
}

export interface EnqueueResponse {
  added: number
  already_studying: number
}

export const readerApi = {
  getSurahs: () =>
    apiClient.get<SurahInfo[]>('/reader/surahs'),

  getAyahs: (surahNumber: number) =>
    apiClient.get<AyahWithWords[]>(`/reader/ayahs/${surahNumber}`),

  enqueue: (wordIds: number[]) =>
    apiClient.post<EnqueueResponse>('/reader/enqueue', { word_ids: wordIds }),

  dequeue: (wordId: number) =>
    apiClient.delete(`/reader/words/${wordId}`),
}
```
  </action>
  <verify>cd frontend && npx tsc --noEmit 2>&1 | grep reader || echo "reader api types ok"</verify>
  <done>reader.ts создан с правильными типами</done>
</task>

<task type="auto">
  <name>Task 3: QuranReaderPage</name>
  <files>frontend/src/pages/QuranReaderPage.tsx</files>
  <action>
Создать frontend/src/pages/QuranReaderPage.tsx.

СТРУКТУРА СТРАНИЦЫ:
- Навбар: лого/назад + dropdown выбора суры
- Прогресс суры: "Аят X / N" + progress bar
- Основной контент: аяты (текущий + несколько следующих видны)
- Арабский текст текущего аята (большой, RTL, с таджвид-цветами)
- Слова кликабельны — каждое отдельный span/button
- Перевод аята на русском
- Навигационные кнопки снизу (fixed на мобильном)
- Легенда таджвида (сворачиваемая)

СОСТОЯНИЕ:
```typescript
const [surahs, setSurahs] = useState<SurahInfo[]>([])
const [selectedSurah, setSelectedSurah] = useState<number>(78) // первая суpa джуза 30
const [ayahs, setAyahs] = useState<AyahWithWords[]>([])
const [currentIndex, setCurrentIndex] = useState(0)
const [pendingWordIds, setPendingWordIds] = useState<Set<number>>(new Set())
// studying: локальная копия is_in_study, обновляется при enqueue/dequeue
const [studyingWordIds, setStudyingWordIds] = useState<Set<number>>(new Set())
const [isLoading, setIsLoading] = useState(true)
const [showLegend, setShowLegend] = useState(false)
```

ЛОГИКА КЛИКА НА СЛОВО:
```typescript
const handleWordClick = async (wordId: number | null, isInStudy: boolean) => {
  if (!wordId) return // функциональное слово без id

  if (isInStudy || studyingWordIds.has(wordId)) {
    // Уже в изучении → убрать
    await readerApi.dequeue(wordId)
    setStudyingWordIds(prev => { const s = new Set(prev); s.delete(wordId); return s })
    setPendingWordIds(prev => { const s = new Set(prev); s.delete(wordId); return s })
    return
  }

  // Переключить pending
  setPendingWordIds(prev => {
    const s = new Set(prev)
    if (s.has(wordId)) s.delete(wordId)
    else s.add(wordId)
    return s
  })
}
```

ПЕРЕХОД НА СЛЕДУЮЩИЙ АЯТ (с авто-добавлением слов):
```typescript
const goToNext = async () => {
  // Добавить pending слова в очередь
  if (pendingWordIds.size > 0) {
    const ids = Array.from(pendingWordIds)
    await readerApi.enqueue(ids)
    setStudyingWordIds(prev => { const s = new Set(prev); ids.forEach(id => s.add(id)); return s })
    setPendingWordIds(new Set())
  }
  if (currentIndex < ayahs.length - 1) {
    setCurrentIndex(i => i + 1)
  }
}

const goToPrev = () => {
  // pending слова НЕ добавляем при отходе назад — просто сбрасываем
  setPendingWordIds(new Set())
  if (currentIndex > 0) setCurrentIndex(i => i - 1)
}
```

KEYBOARD SHORTCUTS (useEffect):
```typescript
useEffect(() => {
  const onKey = (e: KeyboardEvent) => {
    // Не реагировать если фокус на input/select
    if (['INPUT', 'SELECT', 'TEXTAREA'].includes((e.target as Element)?.tagName)) return
    if (e.key === 'ArrowRight' || e.key === 'l' || e.key === 'L' || e.key === ' ' || e.key === 'PageDown') {
      e.preventDefault()
      goToNext()
    }
    if (e.key === 'ArrowLeft' || e.key === 'h' || e.key === 'H' || e.key === 'PageUp') {
      e.preventDefault()
      goToPrev()
    }
  }
  window.addEventListener('keydown', onKey)
  return () => window.removeEventListener('keydown', onKey)
}, [currentIndex, pendingWordIds, ayahs.length]) // зависимости для актуальных goToNext/goToPrev
```

JSX СТРУКТУРА:
```tsx
// Навбар
<nav className="bg-white border-b sticky top-0 z-10">
  <div className="max-w-2xl mx-auto px-4 py-3 flex items-center gap-3">
    <button onClick={() => navigate('/dashboard')}>← Назад</button>
    <select value={selectedSurah} onChange={e => handleSurahChange(Number(e.target.value))}>
      {surahs.map(s => (
        <option key={s.number} value={s.number}>
          {s.name_arabic} — {s.name_english} ({s.total_ayahs} аятов)
        </option>
      ))}
    </select>
  </div>
</nav>

// Прогресс
<div className="max-w-2xl mx-auto px-4 py-2">
  <div className="flex justify-between text-sm text-gray-500 mb-1">
    <span>Аят {currentIndex + 1} из {ayahs.length}</span>
    <span>{pendingWordIds.size} выбрано для изучения</span>
  </div>
  <div className="h-1.5 bg-gray-200 rounded-full">
    <div className="h-1.5 bg-emerald-500 rounded-full transition-all"
         style={{ width: `${((currentIndex + 1) / ayahs.length) * 100}%` }} />
  </div>
</div>

// Карточка аята
<main className="max-w-2xl mx-auto px-4 pb-32">
  // Арабский текст — слова
  <div className="bg-white rounded-2xl shadow-sm border p-6 mb-4" dir="rtl">
    <div className="flex flex-wrap gap-x-3 gap-y-2 justify-end" style={{lineHeight: '2.5'}}>
      {currentAyah.words.map((word, i) => {
        const rule = getTajweedRule(word.arabic)
        const colorClass = TAJWEED_CLASSES[rule]
        const isPending = word.word_id !== null && pendingWordIds.has(word.word_id)
        const isStudying = word.word_id !== null && (word.is_in_study || studyingWordIds.has(word.word_id))

        return (
          <button
            key={i}
            onClick={() => handleWordClick(word.word_id, word.is_in_study)}
            className={`
              font-arabic text-3xl px-1 rounded transition-all
              ${word.word_id ? 'cursor-pointer hover:bg-gray-100' : 'cursor-default'}
              ${isPending ? 'bg-yellow-100 ring-2 ring-yellow-400' : ''}
              ${isStudying ? 'bg-emerald-50 ring-2 ring-emerald-400' : ''}
              ${!isPending && !isStudying ? colorClass : ''}
            `}
            style={{ fontFamily: 'Amiri, serif', fontSize: '2rem', lineHeight: '2' }}
            dir="rtl"
            lang="ar"
            title={word.word_id ? (isStudying ? 'Убрать из изучения' : 'Нажмите чтобы добавить в изучение') : 'Служебное слово'}
          >
            {word.arabic}
            {isStudying && <span className="text-xs align-super text-emerald-500">✓</span>}
          </button>
        )
      })}
    </div>

    // Номер аята
    <div className="text-right text-gray-400 text-sm mt-2">
      ﴿{currentAyah.ayah_number}﴾
    </div>
  </div>

  // Перевод
  <div className="bg-gray-50 rounded-xl px-5 py-4 text-gray-700 text-base leading-relaxed mb-4">
    <span className="text-gray-400 text-xs block mb-1">Перевод (Кулиев)</span>
    {currentAyah.russian_translation || 'Перевод недоступен'}
  </div>

  // Легенда таджвида
  <button onClick={() => setShowLegend(v => !v)} className="text-sm text-gray-400 flex items-center gap-1 mb-2">
    {showLegend ? '▲' : '▼'} Правила таджвида
  </button>
  {showLegend && (
    <div className="bg-white rounded-xl border p-4 grid grid-cols-2 gap-2">
      {TAJWEED_LEGEND.map(({ rule, label, description }) => (
        <div key={rule} className="flex items-start gap-2">
          <span className={`font-bold text-sm ${TAJWEED_CLASSES[rule]}`}>أ</span>
          <div>
            <div className="text-xs font-semibold text-gray-700">{label}</div>
            <div className="text-xs text-gray-500">{description}</div>
          </div>
        </div>
      ))}
    </div>
  )}
</main>

// Навигационные кнопки — FIXED снизу, большие для мобильного
<div className="fixed bottom-0 left-0 right-0 bg-white border-t shadow-lg">
  <div className="max-w-2xl mx-auto flex items-center gap-0">
    // Кнопка "Предыдущий"
    <button
      onClick={goToPrev}
      disabled={currentIndex === 0}
      className="flex-1 flex items-center justify-center gap-2 py-5 text-gray-600 disabled:text-gray-300 hover:bg-gray-50 transition-colors text-lg font-medium"
    >
      ← Предыдущий
    </button>

    // Разделитель + счётчик
    <div className="flex flex-col items-center px-4 text-gray-400 text-xs">
      <span>{currentIndex + 1}/{ayahs.length}</span>
      {pendingWordIds.size > 0 && (
        <span className="text-yellow-600 font-medium">+{pendingWordIds.size} слов</span>
      )}
    </div>

    // Кнопка "Следующий"
    <button
      onClick={goToNext}
      disabled={currentIndex === ayahs.length - 1 && pendingWordIds.size === 0}
      className="flex-1 flex items-center justify-center gap-2 py-5 text-emerald-600 disabled:text-gray-300 hover:bg-emerald-50 transition-colors text-lg font-medium"
    >
      {currentIndex < ayahs.length - 1 ? 'Следующий →' : 'Завершить ✓'}
    </button>
  </div>
  // Подсказка для клавиатуры (только на широком экране)
  <div className="hidden md:block text-center text-xs text-gray-300 pb-1">
    ← → или H / L для навигации · Space для следующего
  </div>
</div>
```

СМЕНА СУРЫ:
```typescript
const handleSurahChange = async (surahNumber: number) => {
  setSelectedSurah(surahNumber)
  setCurrentIndex(0)
  setPendingWordIds(new Set())
  setIsLoading(true)
  const res = await readerApi.getAyahs(surahNumber)
  const ayahsData = res.data
  setAyahs(ayahsData)
  // Собрать все is_in_study
  const studyIds = new Set<number>()
  ayahsData.forEach(a => a.words.forEach(w => { if (w.word_id && w.is_in_study) studyIds.add(w.word_id) }))
  setStudyingWordIds(studyIds)
  setIsLoading(false)
}
```

ВАЖНО: использовать `import type` для всех type-only imports (verbatimModuleSyntax в tsconfig).
Все обработчики событий должны иметь правильные типы.
Компонент должен компилироваться без ошибок TypeScript.
  </action>
  <verify>cd frontend && npm run build 2>&1 | tail -10</verify>
  <done>QuranReaderPage создан, npm run build проходит без ошибок TypeScript</done>
</task>

<task type="auto">
  <name>Task 4: Добавить роут /reader в App.tsx</name>
  <files>frontend/src/App.tsx</files>
  <action>
Прочитать frontend/src/App.tsx и добавить:
1. import QuranReaderPage from './pages/QuranReaderPage'
2. Route: `<Route path="/reader" element={<ProtectedRoute><QuranReaderPage /></ProtectedRoute>} />`
  </action>
  <verify>cd frontend && npm run build 2>&1 | grep -E "error|Error" | head -5 || echo "build ok"</verify>
  <done>Роут /reader добавлен в App.tsx</done>
</task>

<task type="auto">
  <name>Task 5: Добавить кнопку "Читать Коран" в DashboardPage</name>
  <files>frontend/src/pages/DashboardPage.tsx</files>
  <action>
Прочитать frontend/src/pages/DashboardPage.tsx.
Найти кнопку "Учиться" (navigate('/study')).
Добавить рядом кнопку "Читать Коран" → navigate('/reader').

Кнопка должна быть такого же размера и стиля, но другого цвета (например blue/indigo):
```tsx
<button
  onClick={() => navigate('/reader')}
  className="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-semibold py-4 px-6 rounded-xl flex items-center justify-between transition-colors"
>
  <span className="text-lg">Читать Коран</span>
  <span className="text-2xl">📖</span>
</button>
```

Разместить ВЫШЕ кнопки "Учиться" или рядом — на усмотрение агента, главное что оба варианта видны.
  </action>
  <verify>cd frontend && npm run build 2>&1 | tail -5</verify>
  <done>Кнопка "Читать Коран" добавлена на дашборде</done>
</task>

</tasks>

<verification>
- [ ] npm run build без ошибок TypeScript
- [ ] getTajweedRule возвращает правильные классы
- [ ] /reader роут работает
- [ ] Выбор суры загружает аяты
- [ ] Клик по слову → жёлтый фон (pending)
- [ ] Следующий аят → слова добавляются в изучение
- [ ] Fixed кнопки навигации видны снизу
- [ ] Keyboard shortcuts: ← → Space работают
</verification>

<success_criteria>
- Страница /reader полностью функциональна
- Таджвид-цвета отображаются корректно
- Слова добавляются в flashcard queue при навигации
- Навигация удобна на мобильном (большие кнопки) и десктопе (клавиатура)
- TypeScript compiles без ошибок
</success_criteria>

<output>
After completion, create `.planning/phases/08-reader/08-03-SUMMARY.md`
</output>
