---
phase: 08-reader
plan: 01
type: execute
depends_on: []
files_modified:
  - frontend/src/pages/StudyPage.tsx
  - frontend/src/components/QualityButtons.tsx
---

<objective>
Завершить систему свайпов в StudyPage: подключить onSwipe callback из FlashCard (уже готов),
убрать QualityButtons, добавить keyboard shortcuts.

FlashCard.tsx уже обновлён — свайп влево/вправо работает, оверлеи отрисовываются.
Нужно только обновить StudyPage.tsx чтобы использовать новый API карточки.

Purpose: Пользователь свайпает карточки как в Tinder — вправо "знаю" (quality 4), влево "не знаю" (quality 1).
Output: StudyPage работает через свайп, QualityButtons удалён.
</objective>

<context>
FlashCard принимает: card, onFlip?, onSwipe?(direction: 'left'|'right')
onSwipe вызывается после анимации улёта карточки (через 280ms).
FlashCard сам управляет isFlipped state.

Текущий StudyPage: использует isFlipped state + QualityButtons.
Нужно: убрать isFlipped из StudyPage (FlashCard сам управляет), убрать QualityButtons,
добавить handleSwipe который вызывает handleQuality(4) для right и handleQuality(1) для left.

Keyboard: после флипа карточки (onFlip callback = true) → стрелка вправо = знаю, влево = не знаю.
Добавить key={currentCard.word_id} к FlashCard чтобы компонент ресетился при смене карточки.

ВАЖНО: handleQuality уже делает API call и setTimeout(300ms) перед nextIndex.
С свайпом: FlashCard уже анимирует 280ms, потом вызывает onSwipe → handleSwipe → handleQuality.
Убери setTimeout из handleQuality (или оставь 0ms) — задержка уже встроена в флипп-анимацию FlashCard.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Обновить StudyPage.tsx</name>
  <files>frontend/src/pages/StudyPage.tsx</files>
  <action>
Переписать StudyPage.tsx:

1. Убрать import QualityButtons
2. Убрать isFlipped state (FlashCard управляет сам). Добавить isFlipped state обратно только для keyboard shortcuts — получать через onFlip callback.
3. Добавить handleSwipe:
```typescript
const handleSwipe = useCallback((direction: 'left' | 'right') => {
  handleQuality(direction === 'right' ? 4 : 1)
}, [handleQuality]) // handleQuality через useCallback тоже
```
4. В handleQuality: убрать setTimeout — вместо этого сделать синхронно (или 50ms):
```typescript
const handleQuality = useCallback(async (quality: 1 | 2 | 3 | 4) => {
  if (!sessionId || isReviewing) return
  setIsReviewing(true)
  const card = cards[currentIndex]
  try {
    await cardsApi.reviewCard(card.word_id, quality, sessionId)
    if (quality >= 2) setCorrectCount(prev => prev + 1)
  } catch { /* продолжаем */ }

  const nextIndex = currentIndex + 1
  if (nextIndex >= cards.length) {
    // завершить сессию
    try {
      const res = await sessionsApi.completeSession(sessionId)
      setSessionResult(res.data)
    } catch {
      setSessionResult({ ... локальный результат ... })
    }
    setState('completed')
  } else {
    setCurrentIndex(nextIndex)
  }
  setIsReviewing(false)
}, [sessionId, isReviewing, cards, currentIndex, correctCount])
```

5. Keyboard shortcuts (useEffect, зависит от isFlipped):
```typescript
useEffect(() => {
  const onKey = (e: KeyboardEvent) => {
    if (state !== 'studying' || isReviewing || !isFlipped) return
    if (e.key === 'ArrowRight') handleSwipe('right')
    if (e.key === 'ArrowLeft') handleSwipe('left')
  }
  window.addEventListener('keydown', onKey)
  return () => window.removeEventListener('keydown', onKey)
}, [state, isReviewing, isFlipped, handleSwipe])
```

6. В JSX: заменить `<FlashCard card={currentCard} onFlip={setIsFlipped} />` на:
```tsx
<FlashCard
  key={currentCard.word_id}
  card={currentCard}
  onFlip={setIsFlipped}
  onSwipe={handleSwipe}
/>
```

7. Убрать `<QualityButtons ... />` и блок с подсказкой.
   Добавить вместо них (показывать только если isFlipped):
```tsx
{isFlipped && (
  <div className="flex justify-between text-sm text-gray-400 px-4">
    <span>← Не знаю</span>
    <span className="text-xs">или стрелки ← →</span>
    <span>Знаю →</span>
  </div>
)}
{!isFlipped && (
  <p className="text-center text-gray-400 text-sm">
    Нажмите на карточку, чтобы увидеть перевод
  </p>
)}
```
  </action>
  <verify>cd frontend && npm run build 2>&1 | tail -5</verify>
  <done>StudyPage использует свайп, TypeScript компилируется без ошибок</done>
</task>

<task type="auto">
  <name>Task 2: Удалить QualityButtons</name>
  <files>frontend/src/components/QualityButtons.tsx</files>
  <action>
Удалить файл frontend/src/components/QualityButtons.tsx:
```bash
rm frontend/src/components/QualityButtons.tsx
```
  </action>
  <verify>test ! -f frontend/src/components/QualityButtons.tsx && echo "deleted"</verify>
  <done>QualityButtons.tsx удалён</done>
</task>

</tasks>

<verification>
- [ ] npm run build проходит без ошибок
- [ ] QualityButtons.tsx удалён
- [ ] StudyPage.tsx не импортирует QualityButtons
- [ ] FlashCard получает onSwipe prop
- [ ] Keyboard shortcuts работают через useEffect
</verification>

<success_criteria>
- Свайп вправо → quality 4, влево → quality 1
- Клавиши ← → работают после флипа
- Карточка сбрасывается при смене (key prop)
- Сборка без ошибок
</success_criteria>

<output>
After completion, create `.planning/phases/08-reader/08-01-SUMMARY.md`
</output>
